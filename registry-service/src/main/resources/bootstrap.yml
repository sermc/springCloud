spring:
  application:
    name: registry-service
  cloud:
    config:
      uri: http://localhost:8081
      #esto no hace falta porque tengo yo impementado security
      username: root
      password: s3cr3t